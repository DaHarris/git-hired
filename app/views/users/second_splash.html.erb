<h1>OH SHIT ITS <%= @user.name %></h1>
<%= link_to 'GO TO USER PAGE!', user_path(@user) %>

<%= form_for @user, :url => { :action => :git_set } do |f| %>

  <%= f.label :github_id %>
  <%= f.text_area :github_id %>

  <%= f.submit %>
<% end %>
